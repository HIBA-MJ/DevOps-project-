apiVersion: batch/v1
kind: Job
metadata:
  name: devops-job
spec:
  template:
    spec:
      containers:
      - name: devops-container
        image: devops-app:latest
        imagePullPolicy: Never
        command: ["python", "main.py"]
      restartPolicy: Never
  backoffLimit: 2
