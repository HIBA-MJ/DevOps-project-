apiVersion: batch/v1
kind: Job
metadata:
  name: devops-job
spec:
  template:
    spec:
      containers:
      - name: devops-container
        image: hiba1mjd/devops-app:latest
        imagePullPolicy: Never
        command: ["python", "app.py"]
      restartPolicy: Never
  backoffLimit: 2
