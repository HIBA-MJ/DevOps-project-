apiVersion: batch/v1
kind: Job
metadata:
  name: devops-job
spec:
  template:
    spec:
      containers:
      - name: devops-container
        image: devops-image:latest
        imagePullPolicy: IfNotPresent
        command: ["python", "main.py"]
      restartPolicy: Never
  backoffLimit: 2
